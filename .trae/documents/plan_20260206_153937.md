Detailed analysis of the build\_messages function in context.py:

1. Function purpose: Builds complete message list for LLM calls
2. Parameters: history, current\_message, skill\_names, media, channel, chat\_id
3. Process:

   * Creates system prompt with identity, bootstrap files, memory, and skills

   * Adds session context if channel/chat\_id provided

   * Includes conversation history

   * Processes current message with optional media attachments

   * Returns complete message list
4. Media processing: Converts images to base64 for LLM consumption
5. Output: Ready-to-use message list for LLM API calls

